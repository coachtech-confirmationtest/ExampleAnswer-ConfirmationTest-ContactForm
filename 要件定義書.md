## 前提条件
- 環境構築手順および `resources/` 以下の画面・JS資産は事前提供済みであり、ドキュメントでは主にバックエンド要件を定義する。
- 全ルートは `routes/web.php` に集約し、従来型のLaravelパターン（フォームPOST → コントローラー → Blade描画 / リダイレクト）を使用する。
- 認証は Laravel Fortify を利用し、`/admin` 配下および `/contacts/export` は `auth` ミドルウェアで保護される。

## 画面定義
| 画面ID | 画面名称 | パス | 備考 |
| --- | --- | --- | --- |
| SCR01 | お問い合わせフォーム | GET `/` | `ContactController@index` がカテゴリ・タグをDBから取得しフォームを表示。 |
| SCR02 | 入力内容確認 | POST `/contacts/confirm` | `ContactController@confirm` が `StoreContactRequest` でバリデーション後、確認ビューを表示。 |
| SCR03 | お問い合わせ完了 | GET `/thanks` | `ContactController@thanks` が完了メッセージのみを返す。 |
| SCR04 | 管理画面 | GET `/admin` | `auth` 必須。`AdminController@index` が検索・一覧・タグ管理をサーバーサイドで描画。 |
| SCR05 | お問い合わせ詳細 | GET `/admin/contacts/{contact}` | `auth` 必須。`AdminController@show` が詳細ページを表示。 |
| SCR06 | CSVダウンロード | GET `/contacts/export` | `auth` 必須。検索条件に基づき `ContactController@export` がCSVをストリーム返却。 |
| SCR07 | 管理者ログイン | GET `/login` | Fortifyログインビュー。 |
| SCR08 | 管理者登録 | GET `/register` | Fortify登録ビュー。 |

## 機能要件
| EPIC名 | 機能ID | 機能名 | 概要（ビジネスロジック） | 入力条件/制約 | 期待される結果 | バックエンド挙動（箇条書き） | 関連ソース/責務 | 区分 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| お問い合わせ受付（ユーザー向け） | WF01 | フォーム表示 | `/` で問い合わせフォームを表示し入力を開始できる。 | - URL: GET `/`<br>- 認証: 不要 | フォームが正常描画される。カテゴリ・タグが表示される。 | - `ContactController@index` が `Category::all()` `Tag::all()` を取得。<br>- `contact.index` ビューに `categories`, `tags` を渡す。 | - `routes/web.php`<br>- `app/Http/Controllers/ContactController@index`<br>- `resources/views/contact/*.blade.php` | 基本 |
| お問い合わせ受付（ユーザー向け） | WF02 | 入力内容確認 | 入力値をサーバーサイドでバリデーションし、確認画面を表示する。 | - URL: POST `/contacts/confirm`<br>- 認証: 不要<br>- ルール: `StoreContactRequest` | 確認画面でカテゴリ・タグ名称付き内容を確認し、修正へ戻れる。 | - `StoreContactRequest` でバリデーション。<br>- 確認ビューにバリデーション済みデータ、カテゴリ、タグを渡す。<br>- hidden inputで送信データを保持。 | - `app/Http/Controllers/ContactController@confirm`<br>- `app/Http/Requests/StoreContactRequest`<br>- `resources/views/contact/confirm.blade.php` | 基本 |
| お問い合わせ受付（ユーザー向け） | WF03 | お問い合わせ送信 | 確認画面から送信し問い合わせ・タグを登録する。 | - URL: POST `/contacts`<br>- 認証: 不要<br>- ルール: `StoreContactRequest` | 成功時 `/thanks` へリダイレクト。失敗時はバリデーションエラー。 | - `StoreContactRequest` で再バリデーション。<br>- `contacts` と `contact_tag` を保存。<br>- `redirect('/thanks')` を返却。 | - `app/Http/Controllers/ContactController@store`<br>- `app/Models/Contact` `Tag` | 基本 |
| お問い合わせ受付（ユーザー向け） | WF04 | 完了画面表示 | 送信後に完了メッセージとHOMEリンクを提供。 | - URL: GET `/thanks`<br>- 認証: 不要 | 完了メッセージが表示されHOMEリンクで戻れる。 | - `/thanks` ルートで `contact.thanks` を返す。 | - `app/Http/Controllers/ContactController@thanks` | 基本 |
| 問い合わせ運用（管理者向け） | AF01 | 問い合わせ検索・一覧 | 認証済み管理者が条件検索し、7件/頁で一覧・ページングを利用。 | - URL: GET `/admin`（auth必須）<br>- クエリ: keyword/gender/category_id/date | テーブルにお問い合わせ一覧が表示され、ページネーションが動作する。 | - `AdminController@index` で `IndexContactRequest` バリデーション。<br>- `Contact::with(...)->paginate(7)` でデータ取得。<br>- `categories`, `tags` もビューに渡す。 | - `app/Http/Controllers/AdminController@index`<br>- `resources/views/admin/index.blade.php` | 基本 |
| 問い合わせ運用（管理者向け） | AF02 | 詳細参照＆削除 | 一覧から詳細ページを閲覧し、不要な問い合わせを削除する。 | - URL: GET `/admin/contacts/{contact}`<br>- DELETE `/admin/contacts/{contact}`（auth必須） | 詳細ページにカテゴリ・タグが表示され、削除後一覧にリダイレクト。 | - `AdminController@show` で詳細ビュー表示。<br>- `AdminController@destroy` で削除後 `redirect('/admin')`。 | - `app/Http/Controllers/AdminController@show,@destroy`<br>- `resources/views/admin/show.blade.php` | 基本 |
| 問い合わせ運用（管理者向け） | AF03 | CSVエクスポート | 認証済み管理者が検索条件に合致するCSVをダウンロード。 | - URL: GET `/contacts/export` + query（auth必須） | BOM付きCSVがダウンロードされる。 | - `ExportContactRequest` で入力検証。<br>- 条件に従い `Contact` を取得し CSV を出力。 | - `app/Http/Controllers/ContactController@export`<br>- `app/Http/Requests/ExportContactRequest` | 応用 |
| 問い合わせ運用（管理者向け） | AF04 | タグマスタ管理 | 管理者がタグを追加・編集・削除して候補を管理。 | - POST `/admin/tags`（auth必須）<br>- PUT `/admin/tags/{tag}`（auth必須）<br>- DELETE `/admin/tags/{tag}`（auth必須）<br>- ルール: `StoreTagRequest` / `UpdateTagRequest` | タグ操作後、管理画面にリダイレクトされ一覧が最新化される。 | - `TagController@store/update/destroy` でCRUD実行。<br>- 各操作後 `redirect('/admin')` を返却。 | - `app/Http/Controllers/TagController`<br>- `app/Http/Requests/StoreTagRequest` `UpdateTagRequest` | 応用 |
| 管理ユーザー認証・アカウント管理 | AU01 | 管理ユーザー登録 | Fortify登録画面から新しい管理ユーザーを作成。 | - URL: GET/POST `/register`<br>- ルール: `CreateNewUser` | 登録成功後 `/admin` へ遷移。 | - `CreateNewUser` が入力検証と保存。<br>- Fortify設定で `home` を `/admin` に設定。 | - `app/Providers/FortifyServiceProvider`<br>- `app/Actions/Fortify/CreateNewUser`<br>- `resources/views/auth/register.blade.php` | 基本 |
| 管理ユーザー認証・アカウント管理 | AU02 | ログイン/ログアウト | Fortifyログイン画面から認証し、ログアウトできる。 | - URL: GET/POST `/login`, POST `/logout`<br>- RateLimit: 5/分 | ログイン成功で `/admin`、ログアウトでセッション破棄。 | - Fortifyログインビューを提供。<br>- `auth` ミドルウェアで `/admin` `/contacts/export` を保護。<br>- `/logout` POST を処理。 | - `app/Providers/FortifyServiceProvider`<br>- `resources/views/auth/login.blade.php`<br>- `routes/web.php` | 基本 |

## ルート定義 (`routes/web.php`)

### 公開ルート
| HTTPメソッド | URL | コントローラー | 説明 |
| --- | --- | --- | --- |
| GET | `/` | `ContactController@index` | お問い合わせフォーム表示 |
| POST | `/contacts/confirm` | `ContactController@confirm` | 確認画面表示（バリデーション後） |
| POST | `/contacts` | `ContactController@store` | お問い合わせ登録 → `/thanks` へリダイレクト |
| GET | `/thanks` | `ContactController@thanks` | 完了画面表示 |

### 認証必須ルート（`auth` ミドルウェア）
| HTTPメソッド | URL | コントローラー | 説明 |
| --- | --- | --- | --- |
| GET | `/admin` | `AdminController@index` | 管理画面一覧（検索・ページネーション） |
| GET | `/admin/contacts/{contact}` | `AdminController@show` | お問い合わせ詳細 |
| DELETE | `/admin/contacts/{contact}` | `AdminController@destroy` | お問い合わせ削除 → `/admin` へリダイレクト |
| GET | `/contacts/export` | `ContactController@export` | CSVエクスポート |
| POST | `/admin/tags` | `TagController@store` | タグ作成 → `/admin` へリダイレクト |
| PUT | `/admin/tags/{tag}` | `TagController@update` | タグ更新 → `/admin` へリダイレクト |
| DELETE | `/admin/tags/{tag}` | `TagController@destroy` | タグ削除 → `/admin` へリダイレクト |

## データ要件
| ID | データ名 | 説明 | 管理すべき情報 | 必須/応用 | 備考 |
| --- | --- | --- | --- | --- | --- |
| DR01 | お問い合わせ情報 | エンドユーザーの問い合わせ内容。 | 氏名（姓/名）、性別、メール、電話（10〜11桁）、住所、建物名、カテゴリID、本文（120字）、作成日時 | 必須 | `contacts` テーブル。<br>`database/migrations/2026_02_10_040043_create_contacts_table.php` |
| DR02 | カテゴリマスタ | 問い合わせ分類。 | content（<=255） | 必須 | `categories` テーブル。<br>`2026_02_10_040042_create_categories_table.php` |
| DR03 | タグマスタ | 問い合わせに付与する任意タグ。 | name（<=50, ユニーク） | 応用 | `tags` テーブル。<br>`2026_02_10_042329_create_tags_table.php` |
| DR04 | お問い合わせ×タグ | 多対多付与。 | contact_id, tag_id（ユニーク組合せ） | 応用 | `contact_tag` テーブル。<br>`2026_02_10_042338_create_contact_tag_table.php` |
| DR05 | 管理ユーザー | 管理画面ユーザー情報。 | name, email(ユニーク), hashed password, remember_token, email_verified_at（two_factor列は現状未使用） | 必須 | `users` テーブル。<br>Fortifyが使用。 |
| DR06 | 認証補助 | パスワードリセットトークン等。 | password_reset_tokens, personal_access_tokens, failed_jobs | 応用 | Laravel標準マイグレーション。 |

## バリデーション一覧（基本＋応用）
| 対象機能 | 入力項目 | ルール |
| --- | --- | --- |
| お問い合わせフォーム（確認・送信共通） | `first_name`, `last_name` | 必須 / string / max:255 |
| お問い合わせフォーム（確認・送信共通） | `gender` | 必須 / integer / in:1,2,3 |
| お問い合わせフォーム（確認・送信共通） | `email` | 必須 / string / email / max:255 |
| お問い合わせフォーム（確認・送信共通） | `tel` | 必須 / string / `regex:/^[0-9]{10,11}$/` |
| お問い合わせフォーム（確認・送信共通） | `address` | 必須 / string / max:255 |
| お問い合わせフォーム（確認・送信共通） | `building` | nullable / string / max:255 |
| お問い合わせフォーム（確認・送信共通） | `category_id` | 必須 / integer / `exists:categories,id` |
| お問い合わせフォーム（確認・送信共通） | `detail` | 必須 / string / max:120 |
| お問い合わせ送信 | `tag_ids[]` | array / integer / `exists:tags,id` |
| 問い合わせ検索・一覧 | `keyword` | nullable / string / max:255 |
| 問い合わせ検索・一覧 | `gender` | nullable / integer / in:0,1,2,3 |
| 問い合わせ検索・一覧 | `category_id` | nullable / integer / `exists:categories,id` |
| 問い合わせ検索・一覧 | `date` | nullable / date |
| タグマスタ管理 | `name` | 必須 / string / max:50 / unique |
| CSVエクスポート | `keyword` | nullable / string / max:255 |
| CSVエクスポート | `gender` | nullable / integer / in:0,1,2,3 |
| CSVエクスポート | `category_id` | nullable / integer / `exists:categories,id` |
| CSVエクスポート | `date` | nullable / date |
| 管理ユーザー登録 | `name` | 必須 / string / max:255 |
| 管理ユーザー登録 | `email` | 必須 / email / max:255 / unique |
| 管理ユーザー登録 | `password` | Fortify標準（8文字以上・確認用一致） |
| ログイン/ログアウト | `email` | 必須 / email / max:255 |
| ログイン/ログアウト | `password` | 必須 |

## テスト要件
| テストID | 対象機能 | 観点 | 手順/期待結果 |
| --- | --- | --- | --- |
| TC-WF01 | お問い合わせフォーム表示 | 画面表示・初期ロード | `GET /` で `contact.index` が表示。カテゴリ・タグが `@foreach` でサーバーサイド描画される。 |
| TC-WF02 | 入力内容確認 | サーバーサイドバリデーション | `POST /contacts/confirm` でバリデーション。正しい入力で確認画面表示。エラー時はリダイレクトしエラーメッセージ表示。 |
| TC-WF03 | お問い合わせ送信 | フォームPOST・登録 | `POST /contacts` で再バリデーション後、DBに `contacts`・`contact_tag` が作成され `/thanks` へリダイレクト。 |
| TC-WF04 | 完了画面表示 | Thanks画面 | `/thanks` で完了メッセージが表示され、HOMEリンクで `/` へ戻る。 |
| TC-AF01 | 問い合わせ検索・一覧 | 検索・ページング | ログイン後 `GET /admin` で条件検索するとフィルタされた結果がBladeで描画。ページネーションが動作。 |
| TC-AF02 | 詳細参照＆削除 | 詳細表示・削除 | `GET /admin/contacts/{id}` で詳細ページ表示。`DELETE /admin/contacts/{id}` で削除後 `/admin` にリダイレクト。 |
| TC-AF03 | CSVエクスポート | CSVダウンロード | `/admin` で条件指定後エクスポートリンクを押すと `/contacts/export` が同条件で呼ばれ、BOM付きCSVが取得できる。 |
| TC-AF04-1 | タグマスタ管理（登録） | タグ登録 | タグ名入力→追加で `POST /admin/tags` → `/admin` にリダイレクトし一覧更新。重複名でバリデーションエラー。 |
| TC-AF04-2 | タグマスタ管理（編集/削除） | タグ編集/削除 | 編集→ `PUT /admin/tags/{id}` → `/admin` にリダイレクト。削除→ `DELETE /admin/tags/{id}` → `/admin` にリダイレクト。 |
| TC-AU01 | 管理ユーザー登録 | 管理者登録 | `/register` で正しく入力するとユーザーが作成され `/admin` へ。未入力や重複でエラー表示。 |
| TC-AU02 | ログイン/ログアウト | 認証動作 | `/login` で認証成功後 `/admin` を表示し、ログアウトボタンでセッション破棄。 |

## テーブル仕様書

### usersテーブル
| No. | テーブル名 | カラム名 | 型 | PRIMARY KEY | NOT NULL | FOREIGN KEY | 補足 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | users | id | bigint unsigned | :white_check_mark: | :white_check_mark: |  | 自動採番（`database/migrations/2014_10_12_000000_create_users_table.php`） |
| 2 | users | name | varchar(255) |  | :white_check_mark: |  | 管理者氏名 |
| 3 | users | email | varchar(255) |  | :white_check_mark: |  | ユニーク制約あり（ログインID） |
| 4 | users | email_verified_at | timestamp |  |  |  | null許容 |
| 5 | users | password | varchar(255) |  | :white_check_mark: |  | ハッシュ化パスワード |
| 6 | users | remember_token | varchar(100) |  |  |  | remember_token() |
| 7 | users | created_at | timestamp |  |  |  | timestamps |
| 8 | users | updated at | timestamp |  |  |  | timestamps |

### categoriesテーブル
| No. | テーブル名 | カラム名 | 型 | PRIMARY KEY | NOT NULL | FOREIGN KEY | 補足 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | categories | id | bigint unsigned | :white_check_mark: | :white_check_mark: |  | `database/migrations/2026_02_10_040042_create_categories_table.php` |
| 2 | categories | content | varchar(255) |  | :white_check_mark: |  | カテゴリ名 |
| 3 | categories | created_at | timestamp |  |  |  |  |
| 4 | categories | updated_at | timestamp |  |  |  |  |

### contactsテーブル
| No. | テーブル名 | カラム名 | 型 | PRIMARY KEY | NOT NULL | FOREIGN KEY | 補足 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | contacts | id | bigint unsigned | :white_check_mark: | :white_check_mark: |  | `database/migrations/2026_02_10_040043_create_contacts_table.php` |
| 2 | contacts | category_id | bigint unsigned |  | :white_check_mark: | categories.id | onDelete cascade |
| 3 | contacts | first_name | varchar(255) |  | :white_check_mark: |  | 姓 |
| 4 | contacts | last_name | varchar(255) |  | :white_check_mark: |  | 名 |
| 5 | contacts | gender | tinyint |  | :white_check_mark: |  | 1:男性,2:女性,3:その他 |
| 6 | contacts | email | varchar(255) |  | :white_check_mark: |  |  |
| 7 | contacts | tel | varchar(11) |  | :white_check_mark: |  | 10〜11桁（ハイフンなし） |
| 8 | contacts | address | varchar(255) |  | :white_check_mark: |  |  |
| 9 | contacts | building | varchar(255) |  |  |  | nullable |
| 10 | contacts | detail | varchar(120) |  | :white_check_mark: |  |  |
| 11 | contacts | created_at | timestamp |  |  |  |  |
| 12 | contacts | updated at | timestamp |  |  |  |  |

### tagsテーブル
| No. | テーブル名 | カラム名 | 型 | PRIMARY KEY | NOT NULL | FOREIGN KEY | 補足 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | tags | id | bigint unsigned | :white_check_mark: | :white_check_mark: |  | `database/migrations/2026_02_10_042329_create_tags_table.php` |
| 2 | tags | name | varchar(50) |  | :white_check_mark: |  | ユニーク制約あり |
| 3 | tags | created_at | timestamp |  |  |  |  |
| 4 | tags | updated at | timestamp |  |  |  |  |

### contact_tagテーブル
| No. | テーブル名 | カラム名 | 型 | PRIMARY KEY | NOT NULL | FOREIGN KEY | 補足 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | contact_tag | id | bigint unsigned | :white_check_mark: | :white_check_mark: |  | `database/migrations/2026_02_10_046338_create_contact_tag_table.php` |
| 2 | contact_tag | contact_id | bigint unsigned |  | :white_check_mark: | contacts.id | onDelete cascade |
| 3 | contact_tag | tag_id | bigint unsigned |  | :white_check_mark: | tags.id | onDelete cascade |
| 4 | contact_tag | created at | timestamp |  |  |  |  |
| 5 | contact_tag | updated_at | timestamp |  |  |  |  |
| 6 | contact_tag | contact_id+tag_id | unique |  |  |  | 同じ組み合わせを禁止 |

### その他（標準テーブル）
- `password_reset_tokens`, `personal_access_tokens`, `failed_jobs` はLaravel標準マイグレーションの定義に従う。
